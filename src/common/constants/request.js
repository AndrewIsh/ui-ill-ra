import { FormattedMessage } from 'react-intl';

export const REQUEST_SECTIONS = {
  summarySection: 'summarySection'
};

export const REQUEST_SECTION_LABELS = {
  [REQUEST_SECTIONS.summarySection]: <FormattedMessage id="ui-ill-ra.summary" />
};
